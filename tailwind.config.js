// Tailwind v4 Config for supernapkin.com
export default {
	theme: {
		extend: {
			// v4 prefers theme variables in CSS (see src/app.css)
			// We only keep secondary mappings if needed for specific legacy reasons
		}
	}
};
